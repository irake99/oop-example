A simple example of comparison between procedural programming and object-oriented programming.

By calculating summations and averages from students' scores.

```
gcc pp.cpp -o pp && ./pp
```

```
gcc oop.cpp -o oop && ./oop
```
